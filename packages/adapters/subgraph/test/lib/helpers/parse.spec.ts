import { expect } from "@connext/nxtp-utils";

describe("Subgraph:parse", () => {
  describe("#parse", () => {
    it("xcall should be undefined if xcalledTransactionHash is null/undefined", () => {});
    it("xcall should not be undefined if xcalledTransactionHash isn't null/undefined", () => {});
    it("execute should be undefined if executedTransactionHash is null/undefined", () => {});
    it("execute should not be undefined if executedTransactionHash isn't null/undefined", () => {});
    it("reconcile should be undefined if reconciledTransactionHash is null/undefined", () => {});
    it("reconcile should not be undefined if reconciledTransactionHash isn't null/undefined", () => {});
  });
});
